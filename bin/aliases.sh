#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
alias dr-build=${DIR}/build.sh
alias dr-lint=${DIR}/lint.sh
alias dr-pack=${DIR}/pack.sh
alias dr-start=${DIR}/start.sh
alias dr-test=${DIR}/test.sh
